#pragma once
#include "fbxsdk.h"
/*
#include <filesystem>

class FbxLoader
{
private:
	bool m_loaded = false;

	std::string m_fileName;

	FbxManager* m_fbxManager = nullptr;

	float m_scale;

public:
	FbxLoader(std::string fileName, float scale = 1.0f);

	bool Load(std::filesystem::path filePath);

	~FbxLoader();

private:
	void ProcessNode(FbxNode* pNode);

	void ProcessMesh(FbxNode* pNode, FbxMesh* pMesh);
};*/